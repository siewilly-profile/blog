// Fill in your GoatCounter endpoint, then set enabled to true.
export const ENGAGEMENT_CONFIG = {
    enabled: true,
    endpoint: "https://siewilly405.goatcounter.com/count",
    scriptSrc: "https://gc.zgo.at/count.js",
    // Count at most once per page key in this period for the same browser.
    viewThrottleMinutes: 30
};
